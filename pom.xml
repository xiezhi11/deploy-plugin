<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.jenkins-ci.plugins</groupId>
        <artifactId>plugin</artifactId>
        <version>5.28</version>
        <relativePath />
    </parent>

    <artifactId>deploy</artifactId>
    <version>${revision}${changelist}</version>
    <packaging>hpi</packaging>
    <name>Deploy to container Plugin</name>
    <url>https://github.com/jenkinsci/${project.artifactId}-plugin</url>

    <scm>
        <connection>scm:git:https://github.com/${gitHubRepo}.git</connection>
        <developerConnection>scm:git:https://github.com/${gitHubRepo}.git</developerConnection>
        <url>https://github.com/${gitHubRepo}</url>
        <tag>${scmTag}</tag>
    </scm>

    <developers>
        <developer>
            <id>kohsuke</id>
            <name>Kohsuke Kawaguchi</name>
        </developer>
        <developer>
            <id>edobm</id>
            <name>Meikel Bode</name>
        </developer>
        <developer>
            <id>devkiela</id>
            <name>Daniel Barth</name>
        </developer>
        <developer>
            <id>frekele</id>
            <name>Leandro Kersting de Freitas</name>
        </developer>
        <developer>
            <id>jansohn</id>
            <name>Robin Jansohn</name>
        </developer>
    </developers>

    <properties>
        <revision>1.18</revision>
        <changelist>-SNAPSHOT</changelist>
        <!-- https://www.jenkins.io/doc/developer/plugin-development/choosing-jenkins-baseline/ -->
        <jenkins.baseline>2.479</jenkins.baseline>
        <jenkins.version>${jenkins.baseline}.3</jenkins.version>
        <gitHubRepo>jenkinsci/${project.artifactId}-plugin</gitHubRepo>
        <ban-junit4-imports.skip>false</ban-junit4-imports.skip>

        <!-- ============================================================ -->
        <!-- Deployment runtime dependency versions                       -->
        <!-- ============================================================ -->
        <!-- Cargo framework — adapters for Tomcat/JBoss/WildFly/Jetty/GlassFish/WebLogic -->
        <cargo.version>1.10.13</cargo.version>
        <!-- GlassFish deployment-client — remote deployment to GlassFish 3.x/4.x/5.x -->
        <deployment-client.version>5.1.0</deployment-client.version>

        <!-- ============================================================ -->
        <!-- HPI bundled artifacts whitelist (79 artifacts)               -->
        <!--                                                              -->
        <!-- Split by provenance so reviewers can verify each group       -->
        <!-- against the corresponding dependency declaration.            -->
        <!--                                                              -->
        <!-- strictBundledArtifacts=true makes the HPI plugin fail the    -->
        <!-- build when a transitive dependency is NOT listed here.       -->
        <!-- After adding or upgrading a dependency, update the matching  -->
        <!-- group below; otherwise the build will fail at package phase. -->
        <!-- ============================================================ -->
        <hpi.strictBundledArtifacts>true</hpi.strictBundledArtifacts>

        <!-- Cargo (org.codehaus.cargo:*) — 11 artifacts -->
        <bundledArtifacts.cargo>cargo-core-api-container,cargo-core-api-generic,cargo-core-api-module,cargo-core-api-util,cargo-core-container-glassfish,cargo-core-container-jboss,cargo-core-container-jetty,cargo-core-container-tomcat,cargo-core-container-weblogic,cargo-core-container-wildfly,cargo-licensed-dtds</bundledArtifacts.cargo>

        <!-- GlassFish deployment-client and container runtime (org.glassfish.main.*) — 24 artifacts -->
        <bundledArtifacts.glassfish-deploy>admin-cli,admin-util,class-model,classmate,common-util,config-api,config-types,deployment-client,deployment-common,geronimo-j2ee-deployment_1.1_spec,glassfish-api,gmbal,internal-api,jakarta.enterprise.deploy-api,launcher,ldapbp,logging,management-api,nucleus-grizzly-all,scattered-archive-api,security,security-services,simple-glassfish-api,ssl-impl</bundledArtifacts.glassfish-deploy>

        <!-- HK2 DI framework — required by GlassFish at runtime — 7 artifacts -->
        <bundledArtifacts.glassfish-hk2>hk2,hk2-api,hk2-config,hk2-core,hk2-locator,hk2-runlevel,hk2-utils</bundledArtifacts.glassfish-hk2>

        <!-- Grizzly NIO framework — required by GlassFish at runtime — 6 artifacts -->
        <bundledArtifacts.glassfish-grizzly>grizzly-config,grizzly-framework,grizzly-http,grizzly-http-server,grizzly-http2,grizzly-portunif</bundledArtifacts.glassfish-grizzly>

        <!-- PFL (Portable Function Library) — required by GlassFish at runtime — 6 artifacts -->
        <bundledArtifacts.glassfish-pfl>pfl-asm,pfl-basic,pfl-basic-tools,pfl-dynamic,pfl-tf,pfl-tf-tools</bundledArtifacts.glassfish-pfl>

        <!-- Third-party transitive dependencies (Jackson, JNA, commons, etc.) — 25 artifacts -->
        <bundledArtifacts.third-party>aopalliance-repackaged,asm-all,asm-repackaged,commons-codec,commons-compress,commons-discovery,commons-io,hibernate-validator,jackson-annotations,jackson-core,jackson-databind,jackson-module-jaxb-annotations,jakarta.inject,javassist,jaxen,jboss-logging,jdom2,jettison,jna,jna-platform,json-simple,mimepull,stax-api,validation-api,xz</bundledArtifacts.third-party>

        <hpi.bundledArtifacts>${bundledArtifacts.cargo},${bundledArtifacts.glassfish-deploy},${bundledArtifacts.glassfish-hk2},${bundledArtifacts.glassfish-grizzly},${bundledArtifacts.glassfish-pfl},${bundledArtifacts.third-party}</hpi.bundledArtifacts>

        <!--
          === Validation commands ===
          mvn validate                                  — POM structure check (offline if parent POM cached)
          mvn dependency:tree -Dscope=runtime           — show runtime dependency tree (requires network)
          mvn package -DskipTests                       — build HPI, triggers bundledArtifacts strict check (requires network)
          mvn hpi:run                                   — run plugin in local Jenkins (requires network)
          mvn dependency:list -DincludeScope=compile    — list compile-scope dependencies

          Why some commands may not run fully offline:
          - Parent POM (org.jenkins-ci.plugins:plugin:5.28) and BOM require repo.jenkins-ci.org
          - Cargo and GlassFish deployment-client artifacts are not on Maven Central
          - HPI packaging must resolve all transitive dependency JARs
        -->
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.jenkins.tools.bom</groupId>
                <artifactId>bom-${jenkins.baseline}.x</artifactId>
                <version>5054.v620b_5d2b_d5e6</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>

        <dependency>
            <groupId>org.jenkins-ci.plugins</groupId>
            <artifactId>credentials</artifactId>
        </dependency>

        <dependency>
            <groupId>org.codehaus.cargo</groupId>
            <artifactId>cargo-core-api-generic</artifactId>
            <version>${cargo.version}</version>
        </dependency>

        <dependency>
            <groupId>org.codehaus.cargo</groupId>
            <artifactId>cargo-core-container-wildfly</artifactId>
            <version>${cargo.version}</version>
        </dependency>

        <dependency>
            <groupId>org.codehaus.cargo</groupId>
            <artifactId>cargo-core-container-jboss</artifactId>
            <version>${cargo.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.ant</groupId>
                    <artifactId>ant</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.apache.ant</groupId>
                    <artifactId>ant-launcher</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.apache.commons</groupId>
                    <artifactId>commons-lang3</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.codehaus.cargo</groupId>
            <artifactId>cargo-core-container-tomcat</artifactId>
            <version>${cargo.version}</version>
        </dependency>

        <dependency>
            <groupId>org.codehaus.cargo</groupId>
            <artifactId>cargo-core-container-jetty</artifactId>
            <version>${cargo.version}</version>
        </dependency>

        <dependency>
            <groupId>org.codehaus.cargo</groupId>
            <artifactId>cargo-core-container-glassfish</artifactId>
            <version>${cargo.version}</version>
        </dependency>

        <dependency>
            <groupId>org.codehaus.cargo</groupId>
            <artifactId>cargo-core-container-weblogic</artifactId>
            <version>${cargo.version}</version>
        </dependency>

        <dependency>
            <groupId>org.glassfish.main.deployment</groupId>
            <artifactId>deployment-client</artifactId>
            <version>${deployment-client.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.glassfish.main.hk2</groupId>
                    <artifactId>tiger-types</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- test dependencies -->
        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-cps</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-basic-steps</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-job</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-durable-task-step</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-cps</artifactId>
            <classifier>tests</classifier>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jenkins-ci.tools</groupId>
                <artifactId>maven-hpi-plugin</artifactId>
                <extensions>true</extensions>
                <configuration>
                    <compatibleSinceVersion>1.13</compatibleSinceVersion>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>repo.jenkins-ci.org</id>
            <url>https://repo.jenkins-ci.org/public/</url>
        </repository>
    </repositories>

    <licenses>
        <license>
            <name>The MIT license</name>
            <comments>All source code is under the MIT license.</comments>
            <url>https://opensource.org/licenses/mit-license.php</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <pluginRepositories>
        <pluginRepository>
            <id>repo.jenkins-ci.org</id>
            <url>https://repo.jenkins-ci.org/public/</url>
        </pluginRepository>
    </pluginRepositories>

</project>

